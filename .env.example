# Copy to .env for local development only. Do not commit .env.
# Load this value into your development environment before starting the API or EF tooling.
# Replace `development-only-placeholder` locally; never commit a real password.
ConnectionStrings__GovernmentDomainCopilot=Host=localhost;Port=5432;Database=government_domain_copilot;Username=postgres;Password=development-only-placeholder
